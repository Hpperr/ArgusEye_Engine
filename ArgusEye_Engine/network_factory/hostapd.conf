# ARGUS-EYE  - Rogue AP Configuration
# Auto-updated by run_tactical.sh

# Interface will be dynamically set
interface=wlan0

# Driver
driver=nl80211

# SSID - Clone from target
ssid=Hotel_VIP_HighSpeed

# Hardware mode
hw_mode=g

# Channel (auto detect from target)
channel=6

# Security
macaddr_acl=0
auth_algs=1
ignore_broadcast_ssid=0

# WPA2 Configuration
wpa=2
wpa_passphrase=password123
wpa_key_mgmt=WPA-PSK
wpa_pairwise=TKIP
rsn_pairwise=CCMP
